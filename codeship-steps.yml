- name: "Cypress E2E tests"
  service: cypress_codeship_test
  command: ls -la
- name: "Cypress E2E tests"
  service: cypress_codeship_test
  command: npm --version

  # command: $(npm bin)/cypress run
